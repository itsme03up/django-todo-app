services:
  - type: web
    name: django-todo-app
    runtime: python3
    buildCommand: "./build.sh"
    startCommand: "daphne -b 0.0.0.0 -p $PORT mytodo.asgi:application"
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"